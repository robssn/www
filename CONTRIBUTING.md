# Contributing

1. Create a feature branch.
2. Run `npm ci` and `npm test`.
3. Keep pages accessible, responsive and free of external runtime dependencies unless explicitly approved.
4. Do not commit Formspree secrets, customer data or private lead exports.
5. Open a pull request with screenshots and validation results.
